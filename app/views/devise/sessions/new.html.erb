<div class="mx-auto max-w-md rounded-xl border border-slate-200 bg-white p-6">
  <h1 class="mb-4 text-xl font-semibold">Sign in</h1>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "space-y-4" }) do |f| %>
    <div>
      <%= f.label :email, class: "mb-1 block text-sm font-medium" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full rounded-lg border border-slate-300 px-3 py-2" %>
    </div>
    <div>
      <%= f.label :password, class: "mb-1 block text-sm font-medium" %>
      <%= f.password_field :password, autocomplete: "current-password", class: "w-full rounded-lg border border-slate-300 px-3 py-2" %>
    </div>
    <%= f.submit "Sign in", class: "w-full rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white" %>
  <% end %>
  <p class="mt-4 text-sm text-slate-600">No account yet? <%= link_to "Create one", new_registration_path(resource_name), class: "font-medium text-slate-900" %></p>
</div>
